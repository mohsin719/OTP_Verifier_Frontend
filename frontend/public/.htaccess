RewriteEngine On
RewriteBase /

DirectoryIndex index.html

# Serve existing files and folders (login/index.html, _next/static, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Client-side routes fallback
RewriteRule ^ index.html [L]
